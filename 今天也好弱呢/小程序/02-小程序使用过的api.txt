1.获取元素宽高
创建节点选择器
const query = wx.createSelectorQuery()
query.select('#id').boundingClientRect(res => {
  对应的元素信息就在res对象里
}).exec();

2.自定义组件之间的通信-relation
当两个自定义组件的relation产生关联时,可以通过this.getRelationNodes方法获取对方组件实例

3.getCurrentPages函数用于获取当前页面栈的实例,以数组形式按栈的顺序给出,第一个元素为首页,最后一个
元素为当前页面
const pages = getCurrentPages()
const currentPage = getCurrentPages[pages.length - 1];

4.使用自定义tabbar注意点:
  -要给app.json的tabbar设置costom: true
  -根目录下创建costom-tab-bar目录