1.unknow和any的主要区别是,unknow类型会更加严格,在对unknow类型的值执行大多数操作之前
我们必须进行某种形式的检查,而在对any类型的值执行操作之前,我们不必进行任何检查
  比如把value设置成any类型,我们可以直接value.name,而设置成unknow会报错

2.元组越界问题,TypeScript允许向元组中使用数组的push方法插入新元素(必须是元组中定义的类型)
但是在访问这个新元素时会报错